{
  "name": "house-of-joshi-swap",
  "version": "0.0.0",
  "description": "Non-custodial multichain token-swap interface with on-chain fee and minimum-output enforcement.",
  "homepage": "https://swap.thehouseofjoshi.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/queenjoshi/Swap-Simulation.git"
  },
  "packageManager": "pnpm@11.7.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "compile:contracts": "node scripts/check-router-chains.mjs && node scripts/compile-contracts.mjs",
    "deploy:router": "node scripts/deploy-hojswap-router-v2.mjs",
    "router:approve-pair": "node scripts/set-router-spender-approval.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "next": "16.2.9",
    "prettier": "^3.8.3",
    "solc": "0.8.26",
    "typescript": "~5.9.3"
  }
}
